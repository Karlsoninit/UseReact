import React from 'react';

const Start = () => (
  <>
    <p>Start page</p>
    <p>
      This example shows how nested routing works. The route /topics loads the
      Topics component, which renders any further s conditionally on the paths
      :id value.
    </p>
  </>
);

export default Start;
