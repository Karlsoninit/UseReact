import React from 'react';

const HomePage = () => (
  <>
    <p>Home</p>
    <p>
      Quick Start You’ll need a React web app to add react-router.If you need to
      create one, the easiest way to get started is with a popular tool called
    </p>
    <p>
      Create React App.First install create-react-app, if you don’t already have
      it, and then make a new project with it.
    </p>
  </>
);

export default HomePage;
